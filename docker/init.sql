-- =============================================================
-- Eduteka DB — Inicializacion
-- Este script se ejecuta automaticamente al crear la BD
-- =============================================================

-- La BD ya existe (creada por POSTGRES_DB en docker-compose.yml)
-- Agrega aqui tus tablas cuando estes listo
SELECT 'Base de datos inicializada correctamente' AS status;
