[routes]
; Las paginas reales (articulos/, admin/) se sirven como archivos PHP directos
; bajo public/ — el .htaccess las deja pasar sin tocar F3 (RewriteCond !-f).
; Este archivo queda listo para rutas F3 nuevas que se agreguen mas adelante.
GET / = App\Controllers\Home->index
